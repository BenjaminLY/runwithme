<div class="banner" style="background-image: linear-gradient(-225deg, rgba(171,207,230,0.5) 0%, rgba(0,36,61,0.3) 50%), url(<%= image_path 'home.jpg' %>);">
  <div class="banner-content">
    <h1>Let's run !</h1>
    <p>Keep working, still running !</p>
    <br>
    <% if user_signed_in? %>
      <%= link_to 'View events', events_path, class:"btn-treehouse" %>
    <% else %>
      <%= link_to 'Sign in', new_user_session_path, class:"btn-treehouse btn-lg" %>
    <% end %>
  </div>
</div>
