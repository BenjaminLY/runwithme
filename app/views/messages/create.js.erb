
  // new_message.slideDown();
  // // Remove the text from the content textarea.
  // $('#message_content').val('');

var message = $('<%= j render "events/message", message: @message %>').hide();

$('.messages').append(message);
message.slideDown();
 $('#message_content').val('');

var messages = $(".description-container");
messages.animate({ scrollTop: messages.prop("scrollHeight") - messages.height() + 400 }, 1000);
